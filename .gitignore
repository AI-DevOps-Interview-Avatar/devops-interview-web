# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results
playwright-report
blob-report

# On-device model bundles. Half a gigabyte each, fetched at runtime from the
# devops-interview-app releases (DIA-97) — never committed, and never part of
# `dist` either: `npm run build` copies all of `public/`, so anything dropped
# here for a manual run has to be moved out before building.
public/models/
